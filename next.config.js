/** @type {import('next').NextConfig} */
const nextConfig = {
  // All images are local (public/images, public/logo). If you ever reference a
  // remote image, whitelist its host here under images.remotePatterns.
}

module.exports = nextConfig
